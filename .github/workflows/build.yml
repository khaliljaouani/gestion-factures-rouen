Set-Content -Path ".github/workflows/build.yml" -Value @'
name: Build Windows Installer

on:
  workflow_dispatch:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install root deps
        run: npm ci

      - name: Install frontend deps
        run: npm ci
        working-directory: frontend

      - name: Build frontend
        run: npm run build
        working-directory: frontend

      - name: Install backend deps (prod only)
        run: npm ci --omit=dev
        working-directory: backend

      # ⛔ Pas de publication, on force CI=false
      - name: Build Windows (NSIS)
        env:
          CI: "false"
        run: npx electron-builder --win nsis --x64 --publish=never --config.directories.output=dist --log-level=debug

      # (optionnel) aussi un portable pour tester vite
      - name: Build Windows (Portable)
        env:
          CI: "false"
        run: npx electron-builder --win portable --x64 --publish=never --config.directories.output=dist --log-level=debug

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: dist/**

  # Publication Release séparée (uniquement si tag)
  release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build
    runs-on: windows-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: windows-build
          path: dist

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
'@
